/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Example for allowing images from a specific domain if using next/image
  // images: {
  //   domains: ['images.unsplash.com'],
  // },
};

module.exports = nextConfig;
